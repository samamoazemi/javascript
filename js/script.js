// string to number :

var userAge = prompt("سن خود را وارد نمایید")   // string

var userNumberAge = Number(userAge)   // becomes a number (first way)

var userNumberAge = +userAge   // becomes a number (second way)

alert(typeof userNumberAge)   // number


// number to string :

// var userAge = 20

// var userStringAge = String(userAge)

// alert(typeof userStringAge)
